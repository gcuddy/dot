#!/usr/bin/env bash
# JankyBorders config - generated by theme system
# Theme: Tokyo Night

hex_to_argb() { echo "${1/#\#/0xff}"; }

options=(
  style=round
  width=6
  active_color="glow($(hex_to_argb '#7aa2f7'))"
  inactive_color=$(hex_to_argb '#292e42')
  hidpi=on
)

borders "${options[@]}"
