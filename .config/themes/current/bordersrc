#!/usr/bin/env bash
# JankyBorders config - generated by theme system
# Theme: Gruvbox Dark

hex_to_argb() { echo "${1/#\#/0xff}"; }

options=(
  style=round
  width=6
  active_color="glow($(hex_to_argb '#83a598'))"
  inactive_color=$(hex_to_argb '#3c3836')
  hidpi=on
)

borders "${options[@]}"
